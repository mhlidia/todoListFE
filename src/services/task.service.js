const API_URL = "http://127.0.0.1:8000/api/tasks";

export const tareaService = {

    getAll: async () => {
        // se implementará en el Ticket 5
    },

    create: async (data) => {
        // implementación pendiente
    },

    update: async (id, data) => {
        // implementación pendiente
    },

    delete: async (id) => {
        // implementación pendiente
    }

};